# PDF转换�?- Docker镜像构建和部�?
## 🎯 两种部署方案

### 方案A: 在Portainer中直接构建（推荐�?- **优点**: 简单，不需要本地构�?- **缺点**: 构建较慢（需要从源码构建�?
### 方案B: 本地构建后推送到私有仓库
- **优点**: 部署快�?- **缺点**: 需要Docker Registry或Hub账号

---

## 📦 方案A: Portainer在线构建（详细步骤）

### 步骤1: 访问Portainer

打开浏览器访问你的Portainer地址�?```
http://你的Portainer地址:9000
```

### 步骤2: 登录并选择环境

1. 登录Portainer
2. 选择 **"Local"** 或你的目标环�?
### 步骤3: 创建App Template

1. 点击左侧 **"App Templates"** �?**"应用模板"**
2. 如果没有自定义模板，需要使�?**"Stacks"**

### 步骤4: 创建Stack

**点击 "Stacks" �?"Add stack"**

#### 选择构建方式: "Web Editor"

在Web editor中粘贴以下内容：

```yaml
version: '3.8'

services:
  pdf-converter:
    image: node:18-alpine
    container_name: pdf-converter
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - BASE_URL=http://服务器IP:3000
      - CLOUDCONVERT_API_KEY=你的API密钥
    volumes:
      - pdf-uploads:/app/uploads
      - pdf-outputs:/app/outputs
    working_dir: /app
    command: >
      sh -c "npm install express multer cors dotenv uuid axios form-data &&
             node server.js"

volumes:
  pdf-uploads:
    driver: local
  pdf-outputs:
    driver: local
```

### 步骤5: 修改配置

1. �?`服务器IP` 替换为你的服务器实际IP
2. �?`你的API密钥` 替换为你的CloudConvert API密钥（可选）

### 步骤6: 部署

1. Stack名称输入: `pdf-converter`
2. 点击 **"Deploy the stack"**

### 步骤7: 等待完成

- 首次部署会安装npm依赖（约1-2分钟�?- 查看容器状态是否为 **Running**
- 点击容器查看日志确认无错�?
### 步骤8: 访问

打开浏览器访�?
```
http://服务器IP:3000
```

---

## 🐳 方案B: 使用预构建镜像（最简单）

如果你想要更简单的方案，可以使用预构建的Node.js镜像�?
```yaml
version: '3.8'

services:
  pdf-converter:
    image: nodesource/node:18-alpine
    container_name: pdf-converter
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - BASE_URL=http://服务器IP:3000
      - CLOUDCONVERT_API_KEY=你的API密钥
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./server.js:/app/server.js
    working_dir: /app
    command: sh -c "npm install express multer cors dotenv uuid axios form-data && node server.js"
```

---

## 🔧 方案C: 完整的Dockerfile构建

如果你想先在本地构建镜像，然后上传：

### 1. 创建Dockerfile

项目已包�?`Dockerfile`，内容如下：

```dockerfile
FROM node:18-alpine

WORKDIR /app

# 安装构建工具
RUN apk add --no-cache tini

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm install --production

# 复制应用文件
COPY . .

# 创建数据目录
RUN mkdir -p uploads outputs && \
    chown -R node:node /app

# 切换到非root用户
USER node

# 暴露端口
EXPOSE 3000

# 环境变量
ENV PORT=3000

# 健康检�?HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/ || exit 1

# 启动命令
ENTRYPOINT ["/sbin/tini", "--"]
CMD ["node", "server.js"]
```

### 2. 本地构建

```bash
cd e:\mining\mining_proxy\pdf-converter

# 构建镜像
docker build -t pdf-converter:latest .

# 测试运行
docker run -d \
  --name pdf-converter \
  -p 3000:3000 \
  -e CLOUDCONVERT_API_KEY=你的密钥 \
  pdf-converter:latest
```

### 3. 导出镜像

```bash
# 导出为tar文件
docker save -o pdf-converter.tar pdf-converter:latest

# 传输到服务器
scp pdf-converter.tar user@服务器IP:/path/to/

# 在服务器上加�?docker load -i pdf-converter.tar
```

---

## 📋 Portainer部署清单

### 需要的文件�?- �?`Dockerfile` - 已在项目�?- �?`portainer-stack.yml` - 已在项目�?- �?`server.js` - 应用主文�?- �?`package.json` - 依赖配置
- �?`index.html` - 前端页面

### 可选文件：
- ⚙️ `.env` - 包含API密钥（不要上传到公共仓库！）

---

## ⚠️ 注意事项

### 1. 安全�?- **不要**将包含API密钥�?env文件提交到公共仓�?- 使用Portainer的环境变量功能设置敏感信�?
### 2. 端口冲突
- 确保3000端口未被占用
- 如需修改，编辑portainer-stack.yml中的端口映射

### 3. 防火�?- 确保服务器防火墙开�?000端口
- 如果是云服务器，在安全组中也要开�?
### 4. 数据持久�?- uploads和outputs目录已配置为Docker�?- 数据会持久保存，即使容器重启

---

## 🎯 下一�?
1. **打开Portainer** �?访问你的Portainer地址
2. **按照指南创建Stack** �?参考上述步�?3. **部署并测�?* �?访问 http://服务器IP:3000

---

## 📞 遇到问题�?
请告诉我�?1. 你卡在哪一步？
2. 看到什么错误信息？
3. 你的Portainer版本是多少？

我会帮你解决�?
