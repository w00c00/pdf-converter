# API密钥配置指南

## ✅ 问题已修复
代码已更新，现在会正确加载`.env`文件中的API密钥
---

## 📝 配置步骤

### 步骤1: 创建.env文件

在项目目录下创建名为`.env`的文件：

**文件位置**: `e:\mining\mining_proxy\pdf-converter\.env`

**文件内容**:
```env
CLOUDCONVERT_API_KEY=这里填入你的API密钥
BASE_URL=http://localhost:3000
PORT=3000
```

### 步骤2: 获取API密钥

1. 访问 https://cloudconvert.com/api/v2
2. 注册或登录账户
3. 进入 Dashboard → API
4. 创建新的API密钥
5. 复制密钥

### 步骤3: 重启服务

修改.env文件后，需要重启服务器
```bash
# 停止当前服务（Ctrl+C）
# 重新启动
npm start
```

---

## 🔍 检查配置是否正确
启动服务后，查看终端输出：
```
✅ PDF转换器服务运行在 http://localhost:3000
CloudConvert API密钥: 已配置```
如果看到"已配置"，说明API密钥加载成功
---

## ⚠️ 如果仍然显示未配置
### 检查1: .env文件位置
确保`.env`文件在正确的位置
```
e:\mining\mining_proxy\pdf-converter\.env
```

### 检查2: .env文件格式
```env
# 正确格式
CLOUDCONVERT_API_KEY=sk_example1234567890abcdef

# 错误格式（不要加引号）
CLOUDCONVERT_API_KEY="sk_example1234567890abcdef"  ❌
```

### 检查3: 变量名
确保变量名完全匹配：
```env
# 正确
CLOUDCONVERT_API_KEY=xxx

# 错误
CLOUDCONVERT_API_KEY = xxx  ❌
CLOUDCONVERT_API-KEY=xxx    ❌
api_key=xxx                 ❌
```

### 检查4: 重新安装依赖

```bash
npm install dotenv
```

---

## 📊 验证API是否工作

启动服务后，访问 http://localhost:3000

上传一个PDF文件，选择转换格式，然后点击转换
如果API密钥有效，应该能看到转换成功
---

## 💰 CloudConvert免费额度

- **免费额度**: 25分钟/天
- **PDF转Word**: 4积分/次
- **PDF转JPG**: 1积分/次
---

## 🆘 如果遇到问题

1. **检查密钥是否正确**
   - 登录 cloudconvert.com
   - 查看API密钥设置
   - 确认密钥没有被禁用
2. **检查额度是否用完**
   - 登录 cloudconvert.com
   - 查看剩余额度
   - 等待第二天重置或购买套餐

3. **查看错误日志**
   - 终端会显示详细错误信息
   - 告诉我具体的错误信息

---

## 📞 需要帮助？

请告诉我：
1. 你的.env文件内容（隐藏密钥）
2. 终端显示的信息
3. 你尝试的操作
