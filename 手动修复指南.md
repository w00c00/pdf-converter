# PDF转换器 - 修复指南

## ❌ 问题描述

```
Word转换失败: Cannot find module 'pdfjs-dist/legacy/build/pdf'
```

这是因为缺少 `pdfjs-dist` 依赖库
---

## ✅ 解决方案

### 方法1: 一键修复（推荐）
在项目目录下运行以下命令：
```bash
cd e:\mining\mining_proxy\pdf-converter

# 1. 复制正确的配置文件
copy package-free.json package.json

# 2. 安装依赖（可能需要几分钟）
npm install

# 3. 启动服务
npm start
```

### 方法2: 手动安装依赖

如果上述方法失败，尝试手动安装：

```bash
cd e:\mining\mining_proxy\pdf-converter

# 删除旧的node_modules（如果有）
rmdir /s /q node_modules 2>nul
del package-lock.json 2>nul

# 逐个安装核心依赖
npm install express@4.18.2
npm install multer@1.4.5-lts.1
npm install cors@2.8.5
npm install uuid@9.0.1
npm install pdfjs-dist@3.11.174
npm install docx@8.5.0

# 启动
npm start
```

### 方法3: 使用API版本（最简单）

如果免费版本安装失败，直接使用API版本：
```bash
# 恢复API版本配置
copy package.json.bak package.json 2>nul
npm start
```

或者：

```bash
# 使用启动脚本
start.bat
```

---

## 🔧 手动安装pdfjs-dist

如果仍然失败，尝试以下步骤：

### 步骤1: 检查Node.js版本
```bash
node --version
```
确保是 v14.0.0 或更高版本
### 步骤2: 清理npm缓存
```bash
npm cache clean --force
```

### 步骤3: 使用淘宝镜像（国内用户）
```bash
npm install pdfjs-dist@3.11.174 --registry=https://registry.npmmirror.com
```

### 步骤4: 或者使用yarn
```bash
npm install -g yarn
yarn add pdfjs-dist@3.11.174
```

---

## 📋 如果以上都不行
### 选择A: 使用API版本
直接运行：
```bash
start.bat
```

API版本使用CloudConvert云服务，不需要本地安装任何依赖
### 选择B: 只使用文本转换
修改 `server-free.js`，注释掉图片转换功能：
```javascript
// 找到这一行，改为：
if (!['docx', 'txt'].includes(format)) {
    return res.status(400).json({
        success: false,
        error: '只支持TXT和DOCX格式'
    });
}

// 注释掉JPG/PNG相关的代码
// case 'jpg':
// case 'png':
//     result = await convertPdfToImage(req.file, format);
//     break;
```

---

## 🎯 最简单的解决方案

如果以上方法都失败，**直接使用API版本**：
```bash
start.bat
```

这会启动CloudConvert API版本，虽然需要配置API密钥（或者使用每月25分钟的免费额度），但安装最简单
---

## 📞 获取帮助

如果问题仍然存在，请告诉我：

1. 你的Node.js版本（`node --version`）
2. 完整的错误信息
3. 你尝试了哪些方法

我会帮你解决
---

**或者**，直接运行 `start.bat` 使用API版本，虽然不是完全免费，但有每月25分钟的免费额度，足够日常使用
