# PDF转换�?- 免费版本使用指南

## 🎉 好消息：现在有免费版本了�?
项目现在提供**两个版本**，你可以根据需要选择�?
---

## 📦 版本对比

| 特�?| CloudConvert API�?| 免费本地�?|
|------|-------------------|-----------|
| **费用** | 需要付费（$9/月起�?| 完全免费 |
| **API密钥** | 需要配�?| 不需�?|
| **文件大小** | 10MB限制 | 50MB限制 |
| **转换质量** | 专业�?| 良好 |
| **隐私�?* | 文件上传到第三方 | 本地处理，更安全 |
| **速度** | 依赖网络 | 依赖本地性能 |
| **离线使用** | �?需要网�?| �?可以离线 |

---

## 🚀 快速开始（免费版本�?
### 方式1: 一键启动（推荐�?```
双击运行: start-free.bat
```

### 方式2: 手动启动
```bash
cd e:\mining\mining_proxy\pdf-converter

# 复制免费版配�?copy package-free.json package.json

# 安装依赖
npm install

# 启动服务
npm start
```

### 方式3: Docker部署（免费版�?```bash
# 编辑 Dockerfile，替换为以下内容
docker build -t pdf-converter-free .
docker run -p 3000:3000 pdf-converter-free
```

---

## ⚙️ 免费版安装要�?
### Node.js (必需)
```bash
# 检查是否已安装
node --version

# 如果没有，下载安�?# https://nodejs.org/
```

### 可选依�?
#### Windows用户
免费版可能需要以下工具（通常已预装）�?
1. **Python** (用于某些原生模块)
   - 下载: https://www.python.org/
   - 安装时勾�?"Add Python to PATH"

2. **Visual Studio Build Tools** (用于编译原生模块)
   - 下载: https://visualstudio.microsoft.com/downloads/
   - 选择 "Build Tools for Visual Studio"

#### Linux/macOS用户
```bash
# Ubuntu/Debian
sudo apt-get install build-essential python3

# macOS
xcode-select --install
```

---

## 📝 使用说明

### 1. 启动服务
```bash
npm start
```

### 2. 访问网页
打开浏览器访�? **http://localhost:3000**

### 3. 上传PDF文件
- 拖放PDF文件到上传区�?- 或点�?选择PDF文件"按钮
- 支持最�?0MB的PDF文件

### 4. 选择转换格式
- **Word (DOCX)**: 将PDF内容转换为可编辑的Word文档
- **JPG 图片**: 将PDF每一页转换为独立的JPG图片

### 5. 开始转�?- 点击"开始转�?按钮
- 等待转换完成
- 下载转换后的文件

---

## 🔧 故障排除

### 问题1: npm install 失败

**错误信息**:
```
gyp ERR! build error
```

**解决方案**:
```bash
# Windows
npm install --global windows-build-tools
npm install

# 或者使用镜�?npm install --registry=https://registry.npmmirror.com
```

### 问题2: 找不到模�?'canvas'

**错误信息**:
```
Error: Cannot find module 'canvas'
```

**解决方案**:
canvas模块需要编译，可能在Windows上有问题。考虑以下替代方案�?
**方案A: 使用替代PDF�?*
```bash
npm install pdf2pic
```

**方案B: 使用在线API版（需要付费）**
```bash
copy package.json.bak package.json
npm start
```

### 问题3: 转换很慢

**原因**: 
- PDF页数太多
- 服务器配置较�?
**解决方案**:
- 减少PDF页数
- 关闭其他占用资源的程�?
### 问题4: 转换后乱�?
**原因**:
- PDF使用了特殊字�?- PDF是扫描图�?
**解决方案**:
- 使用在线API版（更好的OCR支持�?- 使用Adobe Acrobat等专业工具预处理

---

## 🐳 Docker部署（免费版�?
### 创建Dockerfile-free
```dockerfile
FROM node:18-alpine

# 安装构建工具和Python
RUN apk add --no-cache \
    build-base \
    python3 \
    make \
    g++ \
    && ln -s /usr/bin/python3 /usr/bin/python

WORKDIR /app

COPY package.json .
RUN npm install

COPY . .

RUN mkdir -p uploads outputs && \
    chown -R node:node /app

USER node

EXPOSE 3000

ENV PORT=3000

ENTRYPOINT ["node", "server-free.js"]
```

### 部署命令
```bash
# 构建镜像
docker build -f Dockerfile-free -t pdf-converter-free .

# 运行容器
docker run -d \
  --name pdf-converter \
  -p 3000:3000 \
  -v $(pwd)/uploads:/app/uploads \
  -v $(pwd)/outputs:/app/outputs \
  pdf-converter-free

# 查看日志
docker logs -f pdf-converter

# 停止服务
docker stop pdf-converter
docker rm pdf-converter
```

---

## 💡 使用建议

### 什么时候使用免费版�?- �?个人使用，偶尔转换文�?- �?注重隐私，不想上传文件到第三�?- �?有技术能力处理安装问�?- �?预算有限

### 什么时候使用付费版�?- �?企业大规模使�?- �?需要高质量转换效果
- �?不想处理安装和技术问�?- �?需要技术支�?
---

## 🎯 推荐配置

### 个人使用（免费）
1. 使用免费本地�?2. 处理一般的PDF转换需�?3. 扫描/图片PDF使用在线工具预处�?
### 团队/企业使用
1. 使用CloudConvert API�?2. 购买合适的套餐
3. 配置自动化工作流

---

## 📞 获取帮助

如果在安装或使用过程中遇到问题：

1. 查看错误日志
2. 检查Node.js版本（需要v14+�?3. 确认系统满足安装要求
4. 参考官方文�?
---

## 🔄 版本切换

如果你想切换版本�?
### 从免费版切换到API�?```bash
copy package.json package-free.json
copy package.json.bak package.json
npm install
npm start
```

### 从API版切换到免费�?```bash
copy package.json package.json.bak
copy package-free.json package.json
npm install
npm start
```

---

## �?检查清�?
使用免费版前，确保：

- [ ] Node.js已安装（版本14+�?- [ ] 有稳定的网络连接（首次安装需要）
- [ ] 有足够的磁盘空间（转换大文件需要）
- [ ] 端口3000未被占用

---

## 🎉 快速测�?
安装完成后，测试一下：

1. 打开 http://localhost:3000
2. 准备一个小型PDF文件�?-2页）
3. 拖放到上传区�?4. 选择转换格式
5. 点击转换
6. 下载结果

如果成功，恭喜你！�?
如果失败，查看终端的错误信息，参考上面的故障排除部分�?
---

**祝你使用愉快�?* 🚀
